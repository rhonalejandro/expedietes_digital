{{-- Fila de dato individual en el perfil lateral. Recibe: $icon, $label, $value --}}
<div class="pac-perfil-campo">
    <i class="ti {{ $icon }} pac-perfil-campo__icon"></i>
    <div class="pac-perfil-campo__text">
        <span class="pac-label">{{ $label }}</span>
        <span class="pac-value">{{ $value }}</span>
    </div>
</div>
